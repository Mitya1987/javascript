const menuRef = document.querySelector('.js-cards');
const inputRef = document.querySelector('.js-input');
const url = `https://restcountries.eu/rest/v2/name/`;

export default { menuRef, inputRef, url };
